VERDICT: REVISE
SUMMARY: The core direction is right, but the spec is not complete enough to implement safely. It misses signed-input fields that clearly affect authority/dispatch, misstates the `gap_followup` field set, and does not resolve how a mid-run stale-signoff re-pause preserves the original paused gate.

PART A — CITATION VERIFICATION:
  1. campaign.py:2308-2323 — OK — F1-active path is exactly hash equality: `return "signed" if signoff.get("signed_scope_hash") == live else "stale"`; legacy non-F1 exception remains at 2317-2318.
  2. campaign.py:1894-1907 — OK — initial start computes `status = self._signoff_status()` and pauses `campaign_plan_signoff` when `status != "signed"`.
  3. campaign.py:1692-1697 — OK — resume freshness is checked only under `if reason == "campaign_plan_signoff"`.
  4. campaign.py:1716-1743, 1744-1764, 1702-1715, 1772-1776, 1779-1786, 1816-1834 — OK — each cited branch can return `proceed` with no `_signoff_status()` call.
  5. campaign.py:1511, 1529 — OK — pending gap remediation explicitly requires fresh signed: comment says `plan MUST still be fresh-signed`; code checks `elif self._signoff_status() != "signed"`.
  6. campaign.py:1944, 1992-2002 — OK — `_drive_milestones` reads live `seq` and passes live `functional_acceptance` into `run_unit` without a freshness check.
  7. campaign.py:2214-2226 — OK — `H` contains `version/campaign_id/goal/charter_ref/charter_hash/milestones`; no top-level `gap_followup`.
  8. campaign.py:1096, 786, 1258 — OVERSTATED — `gap_followup` is read live at 1096 and `max_subsprints` gates at 1258, but `_over_budget` at 786 reads `self.budget`, not `gap_followup`; schema/runtime use `gap_followup.max_no_progress_rounds`, not `gap_followup.max_total_spawns/max_wall_clock_minutes`.
  9. campaign.py:1730-1735, 1808-1834 — OK — cursor-advancing resume paths rely on `_commit_dispatch_resolution()` after cursor mutation and before resume audits.

PART B — hole/inventory completeness: INCOMPLETE — The resume/dispatch enumeration covers most `_handle_resume` branches and the normal `_drive_milestones` dispatch, but it misses the backlog-exhausted gap-followup outer loop: `_gap_followup_round` treats `not_fresh_signed` as `GAP_DONE` at 1594-1597, letting `run()` mark the campaign done at 1930-1936 instead of re-pausing for re-sign. The T2-B inventory is also incomplete: `gap_followup.max_no_progress_rounds` is live authority at 1096-1101 and 1280-1283 but absent from the seed set; top-level `budget` is live at 602, 784-792, 1781 and not in H; and plan-controlled git/merge authority fields are live but not in H: `milestone_isolation`, legacy `isolation_strategy`, `trunk_branch` at 603-619, per-milestone `isolation_strategy` at 821-826, merge gate/cleanup policy at 885-895 and 955-966.

BLOCKING (must fix before impl):
  B1. Signed-input inventory misses authority-bearing isolation/merge fields — post-sign edits to `milestone_isolation.merge_prompt_at_close`, `cleanup_policy`, `trunk_branch`, or milestone `isolation_strategy` can change branch/worktree setup, merge prompting, merge target, or cleanup while `signed_scope_hash` stays signed — engine-kit/orchestrator/campaign.py:603-619, 821-850, 885-895, 955-966 — add these fields to the inventory and either bind them into H or explicitly justify them as non-authoritative.
  B2. `gap_followup` field set is wrong/incomplete — the spec names non-existent `gap_followup.max_total_spawns/max_wall_clock_minutes` and omits live `max_no_progress_rounds`, which can increase tolerated non-progress after signoff without hash drift — schemas/campaign-plan.schema.json:65-72; engine-kit/orchestrator/campaign.py:1096-1101, 1280-1283 — bind `max_subsprints` and `max_no_progress_rounds`; classify top-level `budget.*` separately.
  B3. Gap-followup outer loop can still silently finish on stale signoff — `_gap_followup_eligible` returns `not_fresh_signed`, then `_gap_followup_round` returns `GAP_DONE`, and `run()` writes `STATUS_DONE` — engine-kit/orchestrator/campaign.py:1594-1597, 1930-1936 — stale/pre-F1 at backlog exhaustion must re-pause for re-sign, not finish.
  B4. Mid-run stale UX can erase the original pause semantics — “re-pause uniformly as `campaign_plan_signoff`” is unsafe if it overwrites `pause_reason`; mechanism-A driver resume, deliver-followup, merge, and decision-bound checkpoints need their original reason/checkpoint preserved for the post-re-sign resume — engine-kit/orchestrator/campaign.py:812-815, 1744-1764, 1772-1776 — keep the original pause reason and block with a signoff-required status, or add an explicit overlay that restores the original gate after re-sign.
  B5. Placement must be specified before irreversible actions, not just “outside barriers” — for `milestone_merge`, the freshness check must run before `_execute_milestone_merge()` and before cursor advance; for cursor-advancing actions it must run before `_stamp_milestone_outcome()` / cursor mutation / `_commit_dispatch_resolution()`; for gap remediation it must run before persisting `pending_remediation` — engine-kit/orchestrator/campaign.py:1663-1679, 1728-1735, 1816-1830 — make these placement constraints explicit.

NON-BLOCKING / NITS:
  N1. If new top-level fields enter H, `signoff_snapshot_authentic()` must reconstruct the same H from stored `scope_envelope`; otherwise stale rendering/prior-coverage auth breaks. The spec hints at `compute_scope_envelope`, but should require updating the snapshot-auth path too.
  N2. TD3 should say “new mid-run freshness gates are F1-gated; existing legacy `campaign_plan_signoff` behavior remains unchanged.” A helper that ignores non-F1 unsigned plans at `campaign_plan_signoff` would not be byte-identical.
  N3. `autonomy.approved_scope` is a charter field, not a campaign-plan field in this runner; it is already covered by `charter_hash` unless a plan-level field is later introduced.

PART D — decisions: TD1 push back: include `gap_followup.max_no_progress_rounds`, top-level `budget.*`, and isolation/merge fields in the inventory. TD2 push back: reuse the signoff UX only if the original pause reason/checkpoint is preserved. TD3 agree with the additive F1 gate for new checks, with N2’s caveat. TD4 push back until the exact before/after placement in B5 is stated. TD5 agree: re-sign migration is expected once new fields enter H, but messages must name the newly hash-bound field class.

PART E — scope/unblock assessment: This is correctly separated from OW-M3: OW-M3 binds the requirement surface/mode basis, while this Track-2 cycle must make stale signed scope blocking at runtime. As written, T2-A+T2-B would not fully unblock OW-M3 because stale state can still finish through the gap-followup outer loop, and because the signed-input inventory still leaves authority-bearing plan fields unsigned. Once B1-B5 are fixed, the cycle would provide the missing runtime guarantee: a post-sign `surface`/mode flip becomes stale and is blocked before dispatch or resume action.