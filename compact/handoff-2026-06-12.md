---
title: aidazi v4 build — context handoff for new agent
date: 2026-06-12
status: ready-for-Phase-F
last_session_end: end of catch-up (post-rollback); 64 files / 10,706 lines on disk
---

# aidazi v4 Build Handoff

This is a portable context package for a new agent picking up the aidazi v4 framework build. Read sections in order. Do **NOT** clean-slate the working tree. Do **NOT** commit without explicit human approval.

---

## §1 背景 (Background)

**Project**: aidazi is being built as v4 — a multi-agent LLM-first framework. Working dir: `/Users/caoruixin/projects/aidazi/` on branch `v0.2-from-v3.2-plan`.

**Authoritative spec (read-only; do not edit)**:
- `/Users/caoruixin/projects/csagent-latest/compact/framework-plan-v4-2026-06-06.md` — THE SPEC (~1500 lines; 13 sections).
- `/Users/caoruixin/projects/csagent-latest/compact/aidazi-v4-build-plan.md` — THE WALKTHROUGH (per-file source pointers + 7-phase build order; ~14K tokens).
- `/Users/caoruixin/projects/csagent-latest/docs/current/` — csagent donor source docs (read-only).
- `/Users/caoruixin/projects/google-baidu-hermes-autoloop/` — hermes donor source (read-only).

**Recent incident**: a 2026-06-10 session misread the in-progress working tree as scrap, stashed it, wiped, and authored 8 inferior Phase A files. User caught it; rolled back 2026-06-11. A "62-file Phase E" tally that surfaced earlier in conversation is NOT on disk anywhere (exhaustively searched: working tree, all stashes, all branches, worktrees, reflog, dangling commits, filesystem). Current best state = restored 28-file tree + catch-up authored 2026-06-11/12 to reach 64 files.

**v4 framing fundamentals**:
- 5-role chain: **Research / Deliver / Dev / Code Reviewer / Acceptance** + Customer (human, NOT an agent).
- v4 supports 4 tracks: Type A AI agent / Type B agentic workflow / Type C demo / Type A+B hybrid.
- **Two distinct loops** (Constitution §3.7 + §1.7-E forbids conflation):
  - **Concept 1 — Auto Loop**: Type A agent self-improvement; `modules/m-autoloop.md`.
  - **Concept 2 — Delivery Loop** (Δ-18): multi-agent team delivery; `process/delivery-loop.md`. Was named "orchestrator-pattern" pre-v4; renamed.
- Acceptance Agent = **peer-of-Research outcome gate** (NOT pre-release QA).
- Term mapping: csagent `java_guard` ↔ Type B+ `runtime_guard` (same role; different name per stack).

---

## §2 目标 (Goal)

Build aidazi v4 framework per the 7-phase plan (A–G) in `compact/aidazi-v4-build-plan.md` §3. Each file should match its spec row in build-plan §2; cross-references between files must resolve.

**Phases status**:
- Phases A / B / C / D / E / catch-up: **complete** — 64 files on disk (see §5).
- Phase F: **next** (this is where the new agent picks up).
- Phase G: archive snapshots + lessons/.gitkeep + tools/README.md.

---

## §3 已确认事实 (Confirmed facts)

### §3.1 Constitution state (`governance/constitution.md`)

Already at v4 final:
- §1.2 trimmed to one-line pointer at `process/profile-aware-maturity.md` (Δ-14).
- §1.7-A through §1.7-E all present:
  - §1.7-A — single abstraction layer per agent (no dual surfaces; Type A default = single tool-use). csagent example MOVED to `docs/friction-playbook.md` F1.
  - §1.7-B — closure_criterion is 3-component paragraph (positive shape + anti-pattern + anchor phrases); NOT keyword match.
  - §1.7-C — Acceptance spawn isolation: only via Customer paste OR calibration-gated orchestrator.
  - §1.7-D — MANDATORY_CHECKPOINTS non-bypass; **four shapes invalid** (omitted / emptied / disabled / overridden). Semantic override = bypass. Legitimate path: ADD a custom checkpoint alongside default.
  - §1.7-E — Auto Loop and Delivery Loop must be named distinctly in adopter docs.
- §1.8 — self-extension clause: adopters MAY add domain forbidden items; MAY NOT subtract from §1.7.
- §3.3 — "Track applicability" column dropped (moved to Δ-14); table preamble points reader to Δ-14.
- §3.4 invariants — Deliver-no-code clarification: "Deliver MAY draft handoff §0 templates and sprint-prompt scaffolds but does not edit feature code or test code in the dev sandbox."

### §3.2 5-role registry — role-cards/ has exactly 5 agent role cards

- `role-cards/research-agent.md`
- `role-cards/deliver-agent.md`
- `role-cards/dev-agent.md`
- `role-cards/code-reviewer-agent.md`  ← renamed from earlier "review-agent" / never call it "review-agent"
- `role-cards/acceptance-agent.md`

Customer is NOT in role-cards/. Customer-checkpoints catalog lives at `process/customer-checkpoints.md` (Phase B decision — see §4.4). `role-cards/deliver-activation.md` was DELETED (Phase B decision — see §4.4).

### §3.3 Schema conventions

- **Draft 2020-12** for all 9 schemas (`$schema` set).
- `acceptance.on_fix_required.human_confirm_required` is `const: true` in `schemas/mission-charter.schema.json`.
- `acceptance-verdict.schema.json` `evidence_path` pattern is `^eval/runs/.+` (framework default). Adopters using a different evidence dir MUST (a) record divergent row in `docs/current/adoption-state.md` AND (b) maintain local schema override. Both required; without both, validator rejects.
- Schemas are self-contained (no cross-schema `$ref`); each output validates against its own schema.

### §3.4 6-primitive frozen DSL (Δ-6 + `schemas/case-spec.schema.json`)

Portable names (USE THESE; do not switch to csagent's concrete field names in framework spec):

| Portable name | csagent concrete (mapping in Δ-6 §3.5) |
|---|---|
| `tool_call_present(<tool>)` | `accumulated_tool_results.<tool>` |
| `tool_call_order(<a>, <b>)` | `tool_event_seq(<a>) < tool_event_seq(<b>)` |
| `slot_collected(<field>)` | `intake_state.fields_collected.contains(<field>)` |
| `session_flag(<flag>)` | `session.<flag>_present` |
| `any_of(...)` | `any_of(...)` (combinator; same name) |
| `all_of(...)` | `all_of(...)` (combinator; same name) |

4 data primitives + 2 combinators. Grammar is intentionally minimal; rejects keyword/regex/message-content match at parse time (Constitution §1.7-B structural defence).

### §3.5 Fix-layer set wording

**Use**: "universal base layers + profile-specific extension layers."
**Do NOT use**: "9-layer" / "nine layers" anywhere.

Universal base (apply to every track): `infra` / `java_guard` (Type A) / `runtime_guard` (Type B+) / `prompt_projection` / `skill_state` / `semantic_planner` / `eval_spec` / `product_policy` / `judge_calibration` / `human_review_required`.

Profile-specific extensions: `workflow_definition` (Type B / Type A+B hybrid).

Schemas (`review-verdict`, `sprint_stanza`, `deliver-plan-fix`) enumerate the union so a single enum covers all profiles; adopters use the subset relevant to their declared track.

### §3.6 Other normalization conventions applied

- **Δ-11 half-stages** (S1.5 / S2.5 / S3.5) are guidance markers, NOT mandatory gates. §1 of Δ-11 says this explicitly.
- **Δ-12 artifact count**: "14 framework/document artifacts + 1 eval-runtime artifact." Do NOT say "15th artifact"; do NOT collapse to "14."
- **Δ-13** is a HEURISTIC checklist (6 items), NOT a gate. Don't reframe as gate.
- **Δ-15 Part E** (closure_contract finalization step) preserved — load-bearing for Constitution §3.4 invariant #4 (Research-Acceptance contract symmetry).
- **Δ-17-B and Δ-17-C** are concise placeholders (~70 lines each); cite OQ-V4-001/003 + populate triggers; cross-ref Δ-17-A approximations. Do NOT over-expand without evidence.
- **csagent provenance**: front-matter `notes:` only. EXCEPTION: `templates/deliver-close-taxonomy.md` has a non-normative `## Appendix A — Provenance` at the end ("derived from csagent practice, mid-Q2 2026") — keep this as a tolerated exception.
- **Information-only orchestrator checkpoints** are observability events; non-blocking; NOT MANDATORY_CHECKPOINTS; explicitly stated in `process/customer-checkpoints.md` §3.
- **Solo-adopter session boundary**: SHOULD when practical (not MUST). Fallback rules required when not practical — see `process/customer-checkpoints.md` §5.
- **Dev scope drift** routed via `docs/diagnostics/<id>.md`, NOT via a parallel JSON verdict.
- **Code Reviewer §1.7 audit** explicitly includes §1.7-D bypass detection (charter-validator edits; orchestrator state-machine skips) per `role-cards/code-reviewer-agent.md` §7.
- **Acceptance halt-on-no-evidence**: if `evidence_path` inputs are missing OR artifacts empty OR eval harness exited non-zero, halt and route to `gate_hard_fail` MANDATORY_CHECKPOINT. NOT a code-inspection verdict (forbidden per `process/delivery-loop.md` §4.2.8 anti-pattern #5).

### §3.7 Constitution §1.8 hard requirements (cannot be `divergent` in adoption-state.md)

- Constitution §1.7 forbidden list (incl. §1.7-A through §1.7-E).
- Constitution §3.4 5-role boundary invariants.
- `process/delivery-loop.md` §4.2.3 8 MANDATORY_CHECKPOINTS (four-shape non-bypass invariant).
- Constitution §3.6 Acceptance judge calibration gate.

Everything else is suggested per §7.0 (override with rationale in `docs/current/adoption-state.md`).

---

## §4 决策记录 (Decision records)

Decisions made by the user across the build; some via explicit Q-and-A.

### §4.1 Initial framework rebuild strategy (2026-06-07)
- **Decision**: "B then later inverted to clean-slate (incorrectly)" — initial choice was B (preserve old as archive). Later session misread; clean-slated. Rolled back 2026-06-11.
- **Lock-in**: Do NOT clean-slate the working tree.

### §4.2 Constitution: per-track vs single (Phase A correction)
- **Question**: Should Constitution be split into per-track (Type A/B/C) constitutions?
- **Decision**: Option A — "trim, don't split." Keep single universal Constitution; remove ~5% track-specific content.
- **Actions taken** (all already applied to `governance/constitution.md`):
  1. §1.2 trimmed to one-line pointer to Δ-14.
  2. §1.7-A csagent example moved to `docs/friction-playbook.md` F1.
  3. §3.3 dropped "Track applicability" column.
- **Receiving stubs created**: `process/profile-aware-maturity.md` (Δ-14; later expanded in Phase D), `docs/friction-playbook.md` F1 (still stub; F2-F15 pending — see §5 / §6).

### §4.3 §1.7-D non-bypass tightening (Phase A→B boundary)
- **Decision**: §1.7-D MUST cover all 4 evasion shapes: omitted / emptied / disabled / overridden. Semantic override counts as bypass.
- **Applied**: Constitution §1.7-D; `process/delivery-loop.md` §4.2.2 + §4.2.3 + §4.2.8; `process/self-governance.md` §7.0.

### §4.4 Role-cards/ count normalization (Phase B end)
- **Question**: Why 7 role cards when chain has 5 roles?
- **Decision**: Option B. Net result:
  - Move `customer-checkpoints.md` → `process/customer-checkpoints.md` (it's a process catalog, not an agent role).
  - Delete `deliver-activation.md` (deliver-agent.md cold-start section already covers).
- **Action**: `role-cards/` now holds 5 agent role cards. Constitution §11 read-order step 10 explicitly points at `process/customer-checkpoints.md`.

### §4.5 Phase C corrections
- Keep JSON Schema Draft 2020-12.
- Keep `human_confirm_required: const: true`.
- Keep `evidence_path: ^eval/runs/` with explicit adopter-override path (in schema description).
- Reconcile 6-primitive DSL names: portable form (`tool_call_present` etc.); mapping table in Δ-6 §3.5.
- `deliver-close-taxonomy.md`: project-specific sprint/date details removed from normative body; preserved in non-normative Appendix A.
- 9-question + 5 v4 extension `anti-hardcode-review-kernel.md`: kept in ONE file (not split).
- Schemas self-contained (no cross-schema `$ref`).

### §4.6 Phase D fixes
- Fix-layer wording → "universal base layers + profile-specific extension layers."
- Portable 6-primitive names kept; mapping table added.
- Δ-11 half-stages kept as guidance markers (not mandatory gates).
- Δ-12 phrasing → "14 framework/document artifacts + 1 eval-runtime artifact."
- Δ-13 heuristic framing preserved.
- Δ-15 Part E preserved.
- Δ-17-B/C kept concise.
- csagent provenance in front-matter notes only.

### §4.7 Rollback + catch-up (2026-06-11/12)
- **Decision**: Option A — catch up Phase A-E gaps before Phase F.
- **Completed catch-up**: 9 process docs + 1 role card + 2 docs/ files + 8 schemas + 13 templates + AGENTS.md + README.md + .gitignore. Now at 64 files.

### §4.8 Outstanding decision pending (raised end-of-last-turn)
- **Question to user**: Expand `docs/friction-playbook.md` from F1-only stub to full F1-F15 (as Phase F item 41), or keep F1 stub for now?
- **Status**: NOT yet answered. New agent should ask user before either expanding (~600-800 lines) or skipping.

---

## §5 当前任务 (Current task)

### §5.1 On-disk inventory (verified 2026-06-12)

64 files / 10,706 lines total. Directory breakdown:

| Dir | Files | Notes |
|---|---|---|
| `governance/` | 3 | constitution, doc_governance, context_briefing — v4 final |
| `process/` | 25 | All Δ docs + 4 promoted-from-csagent + customer-checkpoints + self-governance + fold-back-protocol + delivery-loop (Δ-18) + worked-example-instance (Δ-7) |
| `modules/` | 3 | m-evaluation, m-trace, m-autoloop |
| `role-cards/` | 5 | acceptance, code-reviewer, deliver, dev, research |
| `schemas/` | 9 | All Draft 2020-12 |
| `templates/` | 13 | 1 yaml (mission-charter) + 12 md |
| `docs/` | 3 | friction-playbook (F1 stub only), directory-taxonomy, two-loops-explainer |
| root | 3 files | AGENTS.md, README.md, .gitignore |
| `examples/` | 0 | EMPTY — Phase F populates |

### §5.2 What's MISSING (Phase F + G)

**Phase F docs/** (7 files):
- `docs/adoption-overview.md` — 5-role mental model; Layer 1/2/3; what framework decides vs adopter; versioning + two loops concept.
- `docs/greenfield-guide.md` — 7-step bootstrap incl Phase 1-5 funnel; §5.3.1 split-or-merge option; Acceptance in first milestone close; STEP 7 Δ-18 charter bootstrap.
- `docs/brownfield-guide.md` — Profile A/B/C inventory + decide + reconcile + validate; adoption-state.md authoring.
- `docs/domain-adaptation.md` — 3 required domain contracts; layer extensions; per-milestone domain ops.
- `docs/friction-playbook.md` — currently F1 stub only; FULL set is F1-F12 (carry from old) + F13 Acceptance calibration regression + F14 closure_contract vs closure_criterion confusion + F15 Phase 1+2 merge regret. **PENDING USER DECISION** — see §4.8.
- `docs/industry-mapping.md` — map v4 to LangGraph / AutoGen / etc.; Pattern 9 F5 evidence as v4 distinguishing.
- `docs/application-funnel.md` — Phase 1-5 funnel reference; complements greenfield-guide.

**Phase F examples/** (per build-plan §2.8):
- `examples/minimal-greenfield/` — WORKING consumer template (live; NOT a donor snapshot):
  - `AGENTS.md`
  - `docs/current/{domain_taxonomy, runtime_invariants, eval_acceptance_bars, agent_context_guide, adoption-state}.md`
  - `docs/{milestone_objective, sprint_objective, 10-handoff, action_bank}.md`
  - `docs/research-briefs/_placeholder.md`
  - `docs/acceptance-reports/_placeholder.md`
  - `docs/diagnostics/failure-briefs/_placeholder.md`
  - `compact/_placeholder.md`
  - `eval/bad_cases/_manifest.md`
- `examples/csagent-reference/_build-trigger.md` — trigger doc per build-plan §5.1.
- `examples/hermes-reference/_build-trigger.md` — trigger doc + build prompt per build-plan §5.2.
- `examples/fortunes-reference-placeholder/_placeholder.md` — Type C placeholder.

**Phase G top-level + archive** (per build-plan §3 Phase G):
- `lessons/.gitkeep` (empty dir marker).
- `tools/README.md` (notes referenced-but-not-built scripts; tracks OQ-V4-009).
- `archive/2026-06-06-v3.2-snapshot.md` ← copy from `csagent-latest/compact/framework-plan-v3.2-2026-06-06.md`.
- `archive/2026-06-06-v4-skeleton.md` ← copy from `csagent-latest/compact/framework-plan-v4-2026-06-06-skeleton.md`.
- `archive/2026-06-06-v4-plan.md` ← copy from `csagent-latest/compact/framework-plan-v4-2026-06-06.md`.
- `archive/2026-06-07-v4-build-plan.md` ← copy from `csagent-latest/compact/aidazi-v4-build-plan.md`.

---

## §6 下一步 (Next step)

In order:

1. **Read this handoff in full + then read** `/Users/caoruixin/projects/csagent-latest/compact/framework-plan-v4-2026-06-06.md` §5 (Application Guide) + §6 (doc tree) since Phase F lands the Application Guide.

2. **Ask user to resolve §4.8 pending question**: friction-playbook full F1-F15 expansion OR keep F1 stub?

3. **Begin Phase F per build-plan §3 step 37-47 order**:
   - `docs/adoption-overview.md` first (most-referenced; salvages framing from old aidazi if available).
   - Then greenfield + brownfield (these are the user-facing entry points; sized 6-12 KB each).
   - Then domain-adaptation + friction-playbook (if expansion approved) + industry-mapping + application-funnel.
   - Then `examples/minimal-greenfield/` working template.
   - Then 3 example trigger / placeholder files.

4. **After Phase F**, surface interpretation decisions and wait for user approval before Phase G.

5. **For each Phase F file**:
   - Read build-plan §2.5 row for source pointers.
   - Check Constitution §-references + Δ-N cross-refs resolve to actual files on disk.
   - Match the conventions in §3 above (e.g., "universal base layers + extension layers" wording; portable 6-primitive names; "14 framework/document + 1 eval-runtime artifact" phrasing).

---

## §7 注意事项 (Caveats)

### §7.1 Hard rules

- **NO clean-slate.** The repo state is real; do not wipe.
- **NO commits without explicit human approval.** User has been explicit on this throughout.
- **NO editing read-only sources**: `/Users/caoruixin/projects/csagent-latest/` and `/Users/caoruixin/projects/google-baidu-hermes-autoloop/`.
- **NO writing `aidazi-v4-rebuild` memory updates without user prompt** — the memory file already records the clean-slate incident.

### §7.2 RTK shell quirks

The user has the RTK token-killer proxy installed. It intercepts `git diff` / `git stash show --stat` / some `find` flags (e.g., `-not` + `-exec`) and returns placeholders or errors. Workarounds:
- Use `\find` to bypass RTK on find.
- Use `git ls-tree` or temp-dir extraction (`git archive | tar -x`) to inspect stash contents.
- Compound find predicates: use plain shell or extract to temp first.

### §7.3 Stashes (do not drop without user OK)

- `stash@{0}` — inferior fresh Phase A from misread (8 files / 2745 L; superseded).
- `stash@{1}` — pre-clean-slate original state (= what was restored to working tree; kept as belt-and-suspenders).

### §7.4 Outstanding open questions (OQs from spec)

- **OQ-V4-001** — Δ-18 Type B full spec deferred until hermes first SOP milestone closes; placeholder in delivery-loop §4.2.4 + Δ-17-B + profile_type_b charter overlay.
- **OQ-V4-003** — Type C populated when first Type C lifecycle completes (`fortunes-reference-placeholder`).
- **OQ-V4-004** — framework versioning policy TBD at first release (semver suggested).
- **OQ-V4-005** — lessons submission mechanism TBD (PR / direct commit / slash command).
- **OQ-V4-006** — adoption-state cadence: suggested "per milestone close"; confirm at first adopter use.
- **OQ-V4-007** — calibration cost on model swap; defer to first multi-model adopter.
- **OQ-V4-008** — `cell_size_target: 500` suggested per Constitution §7.0; revisit after fold-back.
- **OQ-V4-009** — `tools/precommit_bundling_check.sh` etc. referenced in friction-playbook but not built; backlog.

### §7.5 Spec ambiguities flagged during build (resolved, but might recur)

- Δ-3 decision #1 "abstraction layer" default = single tool-use for Type A per Constitution §1.7-A; Type B may default to single action-enum (SOP runner as controller).
- Δ-6 6-primitive DSL extension procedure: adopters add Tier-3 primitives in domain-specific grammar; framework's 6 stay frozen.
- §5.3.1 Phase 1+2 split-or-merge: merged for Type C (always) + seed/single-author projects; split for medium+ complexity + multi-stakeholder.
- Acceptance fix_required route options MAY be narrowed by adopter but MUST be non-empty (charter validator rejects empty `route_options`).

### §7.6 Cross-cutting style conventions

- Headings use `#`/`##`/`###`/`####`/`#####` per nesting; `§N.N.N` numbering in headings (`### §3.4 Role boundary invariants`).
- Front-matter for every governed doc has: `title`, `doc_tier`, `doc_category`, `status`, `implementation_status`, `source_of_truth`, `last_reviewed`, `review_cadence`, `supersedes`, `superseded_by`, `load_discipline`, `size_target`, `split_trigger` (where applicable), `notes` (yaml `>` folded block).
- Cross-references: backtick-wrap paths (`process/delivery-loop.md` §4.2.3); never bare paths.
- Numbered Δ references: `Δ-N` (Greek capital delta + dash + number).
- "Constitution" capitalized when referring to governance/constitution.md; "the constitution" lowercase when generic.

### §7.7 File counts to expect after Phase F + G

- Phase F adds ~10-15 files (7 docs + ~5-7 examples/minimal-greenfield/ files + 3 example triggers).
- Phase G adds ~6 files (lessons/.gitkeep + tools/README.md + 4 archive snapshots).
- Total target: ~80 files (the build-plan §1 named ~70; v4 came in slightly over).

### §7.8 Where to surface interpretation decisions

After each phase (D / E / F), I have surfaced 6-8 interpretation decisions for user approval before the next phase. The new agent should do the same after Phase F. Pattern: brief table or numbered list; specific file paths + line areas; honest about what was inferred vs spec-mandated; offer flag-or-keep options.

---

## §8 已交付文件清单 (Manifest of delivered files for cross-check)

Run from aidazi/ root to verify on-disk state matches this handoff:

```bash
\find . -type f -not -path './.git/*' -not -path './compact/*' | wc -l   # → 64
```

Per-directory (file names; alphabetical within dir):

```
governance/
  constitution.md
  context_briefing.md
  doc_governance.md

process/
  agent-creation-prerequisites.md          (Δ-16)
  agent-design-elicitation.md              (Δ-15)
  architecture-health-metrics.md           (promoted)
  artifact-taxonomy.md                     (Δ-12)
  badcase-lifecycle.md                     (promoted)
  capability-staging-roadmap.md            (Δ-11)
  common-detours-and-warnings-typeA.md     (Δ-17-A)
  common-detours-and-warnings-typeB.md     (Δ-17-B placeholder)
  common-detours-and-warnings-typeC.md     (Δ-17-C placeholder)
  context-passing-efficiency.md            (Δ-5)
  customer-checkpoints.md                  (human-side gates)
  delivery-loop.md                         (Δ-18 — the largest doc)
  doc-lifecycle-rules.md                   (Δ-4)
  doc-responsibility-matrix.md             (Δ-10)
  domain-discovery-process.md              (Δ-2)
  fold-back-protocol.md
  milestone-framework.md                   (promoted)
  post-deployment-iteration.md             (Δ-9)
  profile-aware-maturity.md                (Δ-14)
  prompt-artifact-rules.md                 (promoted)
  self-governance.md
  stage-stable-heuristic.md                (Δ-13)
  tech-architecture-decision-catalog.md    (Δ-3)
  typeA-runtime-architecture-skeleton.md   (Δ-6)
  worked-example-instance.md               (Δ-7)

modules/
  m-autoloop.md          (Concept 1)
  m-evaluation.md
  m-trace.md

role-cards/
  acceptance-agent.md
  code-reviewer-agent.md
  deliver-agent.md
  dev-agent.md
  research-agent.md

schemas/
  acceptance-verdict.schema.json
  adoption-state.schema.json
  case-spec.schema.json
  deliver-close-verdict.schema.json
  deliver-plan-fix.schema.json
  mission-charter.schema.json
  research-brief.schema.json
  review-verdict.schema.json
  sprint_stanza.schema.json

templates/
  adoption-state-template.md
  anti-hardcode-review-kernel.md
  compact-acceptance-prompt.md
  compact-codex-rebuttal-prompt.md
  compact-dev-prompt.md
  compact-research-brief.md
  compact-review-prompt.md
  deliver-close-taxonomy.md
  handoff-template.md
  lessons-learned-template.md
  milestone-objective.md
  mission-charter.yaml
  sprint-objective.md

docs/
  directory-taxonomy.md
  friction-playbook.md     ← only F1 stub authored (PENDING decision)
  two-loops-explainer.md

(root)
  .gitignore
  AGENTS.md
  README.md
```

If any file in this list is missing OR if file count ≠ 64, the working tree has drifted from this handoff state. Investigate before proceeding.

---

End of handoff.
