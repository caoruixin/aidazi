# fortunes-reference — placeholder (Type C)

**Status**: empty placeholder. This slot holds a Type C (demo/POC) worked example, populated when the first Type C project completes a full lifecycle.

Resolves OQ-V4-003. A Type C reference would show:

- An off-the-shelf skill inventory (Type C leans on pre-built skills; little custom authoring).
- A `LOCAL_ACCEPTANCE_CHECKLIST.md` as the acceptance bar (instead of a full eval suite).
- Acceptance run every demo (Type C cadence per `process/profile-aware-maturity.md`, Δ-14).
- A `profile_type_c` charter overlay (`off_the_shelf_skill_inventory_required: true`).

## Trigger

The first Type C adopter completes a lifecycle and contributes a snapshot, OR a fold-back sub-sprint prioritizes a Type C reference. Name the populated dir `examples/fortunes-reference-YYYY-MM-DD/` (Δ-7 read-only-after-snapshot).

---

Until populated, Type C adopters use the Type C column in `process/profile-aware-maturity.md` (Δ-14) + `docs/domain-adaptation.md`.
