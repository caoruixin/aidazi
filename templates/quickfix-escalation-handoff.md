# Quick-Fix escalation handoff — `<request_id>`

> The Quick-Fix lane **stopped and escalated**. No result was applied to any branch the
> human uses. Per `process/quickfix-lane.md` §5/§11, the investigation below was
> **preserved before the worktree was torn down**. **Relaunch this work in Full
> framework mode** — the Quick-Fix lane is the wrong instrument for it.

## Why it escalated

- **Trigger:** `<one of: scope_expansion | protected_surface_hit | verification_failure |
  unknown_semantic_or_new_design_choice | unprovable_eligibility | uncertainty>`
- **Detail:** `<concise, factual reason — e.g. "touched governance/** (protected:
  governance)", or "rename destination outside allowed_globs", or "introducing a new
  routing decision the LLM should own (Constitution §1.7)">`
- **Detected at:** `<preliminary_guard | edit | targeted_verification | final_guard>`

## What was attempted

`<one paragraph: what the fix tried to do, against the original task_summary>`

## Preserved investigation (NOT lost to teardown)

- **Baseline SHA:** `<worktree HEAD captured at launch>`
- **Saved patch:** `<patch_path — outside the ephemeral worktree>` (hash `<patch_hash>`)
- **Diff summary:**

```
<git --stat / short diff summary of the preserved work>
```

- **Record line:** appended to `.orchestrator/quickfix/records.jsonl` with
  `outcome: escalated`.

## What the human should do next

1. Review the saved patch and this handoff.
2. **Relaunch in Full framework mode** (a normal session at the adopter repo root) and
   carry this work through the proper chain — the trigger above indicates it needs
   Research/Deliver scope, a protected-surface review, or Acceptance, not a quick fix.
3. Do **not** re-attempt it as a Quick Fix by widening `allowed_globs` to dodge the
   trigger — that is the exact move the lane is designed to refuse.

---

*Generated by the Quick-Fix lane. This handoff is an audit artifact, not an
authorization; it does not activate anything.*
